# TFGrid ERPNext Configuration
# Copy this file to .env and customize

# Required: Public domain for ERPNext
DOMAIN=erp.example.com

# Optional: Email for Let's Encrypt SSL certificates
SSL_EMAIL=admin@example.com

# Optional: Site name (default: derived from domain)
# SITE_NAME=erp.example.com

# Optional: Admin password (auto-generated if not set)
# ADMIN_PASSWORD=your-secure-password

# Optional: Database password (auto-generated if not set)
# DB_PASSWORD=your-db-password

# Optional: ERPNext version (default: latest)
# ERPNEXT_VERSION=v15

# Optional: Timezone
# TIMEZONE=UTC
